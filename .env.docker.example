# ============================================
# Strapi CMS - Docker Environment Variables
# ============================================
# INSTRUCTIONS:
# 1. Copy this file to .env in sixthgear-cms folder
# 2. Generate secrets using the commands below
# 3. Database settings are automatically set in docker-compose.yml
# 4. NEVER commit .env to git
# ============================================

# ============================================
# DATABASE (Local Postgres in Docker)
# ============================================
# These are automatically set in docker-compose.yml
# Only override if you need different values
DATABASE_CLIENT=postgres
DATABASE_HOST=strapi-db
DATABASE_PORT=5432
DATABASE_NAME=strapi
DATABASE_USERNAME=strapi
DATABASE_PASSWORD=strapipassword
DATABASE_SSL=false

# ============================================
# SERVER CONFIGURATION
# ============================================
HOST=0.0.0.0
PORT=1337
NODE_ENV=development

# ============================================
# SECURITY SECRETS
# ============================================
# Generate using these commands:
# node -e "console.log(require('crypto').randomBytes(16).toString('base64'))"  # For APP_KEYS (run 4 times)
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"  # For other secrets

# APP_KEYS: Comma-separated list of 4 keys (no spaces)
# Generate 4 keys and join with commas
APP_KEYS=<REQUIRED - 4 base64 keys separated by commas>
# Example: APP_KEYS=key1==,key2==,key3==,key4==

# API Token Salt (for API tokens)
API_TOKEN_SALT=<REQUIRED - base64 string>

# Admin JWT Secret (for admin authentication)
ADMIN_JWT_SECRET=<REQUIRED - base64 string>

# Transfer Token Salt (for data transfer)
TRANSFER_TOKEN_SALT=<REQUIRED - base64 string>

# JWT Secret (for general JWT operations)
JWT_SECRET=<REQUIRED - base64 string>

# ============================================
# ADMIN PANEL CONFIGURATION
# ============================================
# Admin panel URL (leave empty for default /admin)
ADMIN_PATH=/admin

# ============================================
# CORS & SECURITY
# ============================================
# Allowed origins for API requests
# Include your frontend domains
STRAPI_CORS_ORIGINS=http://localhost:3000,https://yourapp.vercel.app

# ============================================
# CLOUDFLARE R2 / S3 STORAGE (Optional)
# ============================================
# If you want to use R2 for Strapi uploads
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=auto
# AWS_BUCKET=sixthgear-strapi-uploads
# AWS_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
# AWS_PUBLIC_URL=https://cdn.yourdomain.com
